 function openPage()
               {
                   var x = document.getElementById("facul").value;
                   
                   if (x === "SCIT"){
                       window.open("scit.html");
                   }

                    if (x === "FENC") {
                       window.open("fenc.html");
                   }

                    if (x === "COBAM") {
                       window.open("cobam.html");
                   }

                    if (x === "COHS") {
                       window.open("cohs.html");
                   }

                    if (x === "FOBE") {
                       window.open("fobe.html");
                   }

                    if (x === "FELS") {
                       window.open("scit.html");
                   }

                    if (x === "FOSS") {
                       window.open("scit.html");
                   }

                    if (x === "FOL") {
                       window.open("scit.html");
                   }
               }